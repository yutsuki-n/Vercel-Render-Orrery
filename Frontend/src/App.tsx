import { Button } from "@/components/ui/button";

function App() {
  return (
    <div className="min-h-screen bg-blue-500 flex items-center justify-center text-white text-3xl">
      Tailwind 動作確認中
      <Button>shadcn Button Test</Button>
    </div>
  );
}

export default App;
